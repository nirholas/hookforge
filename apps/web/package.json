{
  "name": "@hookforge/web",
  "version": "1.0.0",
  "private": true,
  "description": "hookforge.pages.dev: the 3D catalogue and documentation for the HookForge Uniswap v4 hooks.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "vite build && node build.mjs",
    "dev": "vite build --watch & node build.mjs",
    "preview": "node build.mjs && npx serve dist",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@hookforge/registry": "1.0.0"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "three": "^0.170.0",
    "vite": "^5.4.0"
  }
}
